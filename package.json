{
  "name": "perpage",
  "version": "1.0.0",
  "scripts": {
    "source-bashrc": "sh -c 'source ~/.bashrc || true; source ~/.cargo/env || true'",
    "install-trunk": "curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y && cargo install trunk",
    "build": "npm run source-bashrc && npm run install-trunk && npm run source-bashrc && trunk build --release"
  },
  "devDependencies": {
    "wasm-pack": "^0.10.0"
  }
}
